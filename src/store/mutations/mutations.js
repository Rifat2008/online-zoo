export default {
    SET_PETS_TO_STATE: (state, pets) => {
        state.pets = pets;
    },
    SET_TESTIMONIALS_TO_STATE: (state, testimonials) => {
      state.testimonials = testimonials;
    }
}
